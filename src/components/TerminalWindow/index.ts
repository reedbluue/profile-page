import {TerminalButton} from "./TerminalButton.tsx";
import {TerminalBar} from "./TerminalBar.tsx";
import {TerminalBody} from "./TerminalBody.tsx";

export const Terminal = {
  Bar: TerminalBar,
  Button: TerminalButton,
  Body: TerminalBody
}